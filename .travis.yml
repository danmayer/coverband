language: ruby
rvm:
  - "2.3"
  - "2.4"
  - "2.5"
  - "2.6"
gemfile:
  - Gemfile
  - Gemfile.rails4
services:
  - redis-server
before_install:
    - gem update --system
    - gem install bundler -v 1.17.3
    - gem which bundler
    - bundle -v
